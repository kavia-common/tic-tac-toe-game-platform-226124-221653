# Frontend environment variables
# API base URL used by the frontend to reach the backend service.
# If unset, the app defaults to http://localhost:3001
REACT_APP_API_BASE_URL=http://localhost:3001

# Optional legacy/alternate variables that may be present in some environments
# REACT_APP_BACKEND_URL=
# REACT_APP_API_BASE=
